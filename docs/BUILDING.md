# Building DeshiOS

The repository contains locations for DeshiOS build configuration and scripts. Add the exact tested build procedure from the real DeshiOS builder before claiming reproducible builds.

Never commit generated ISO files, chroot trees, caches, credentials, private keys or secrets.
